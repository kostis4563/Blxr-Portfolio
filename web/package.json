{
  "name": "@blxr/web",
  "private": true,
  "version": "0.0.0",
  "license": "UNLICENSED",
  "type": "module",
  "scripts": {
    "locales": "node build-locales.js",
    "predev": "npm run locales",
    "dev": "vite",
    "prebuild": "npm run locales",
    "prebuild:protected": "npm run locales",
    "build:client": "vite build",
    "build:server": "vite build --ssr src/entry-server.jsx --outDir dist-ssr --emptyOutDir",
    "prerender": "node prerender.js",
    "compress": "node compress.js",
    "build": "npm run build:client && npm run build:server && npm run prerender && npm run compress",
    "build:protected": "OBFUSCATE=1 npm run build:client && npm run build:server && npm run prerender && npm run compress",
    "preview": "vite preview"
  },
  "dependencies": {
    "@tailwindcss/vite": "^4.3.3",
    "gsap": "^3.15.0",
    "react": "^19.2.7",
    "react-dom": "^19.2.7",
    "tailwindcss": "^4.3.3"
  },
  "devDependencies": {
    "@types/react": "^19.2.17",
    "@types/react-dom": "^19.2.3",
    "@vitejs/plugin-react": "^6.0.3",
    "javascript-obfuscator": "^5.5.0",
    "vite": "^8.1.1"
  }
}
